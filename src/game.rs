#![allow(dead_code)]
use crate::block::*;
use crate::blocks::*;
use crate::grid::Grid;
pub struct Game {
    pub grid: Grid,
    blocks: Vec<BlockStruct>,
}

impl Game {
    fn new() -> Self {
        todo!();
    }
}
